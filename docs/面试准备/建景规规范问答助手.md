# 面试准备 · 建景规规范问答助手

> **用途**：面试主力项目（toB / RAG 最硬证据）的口径稿。逐题对应《面试内容梳理》第 3 节（项目题）+ 第 4 节（项目梳理）。
> **写法**：第一人称、事实密集、可直接念。重点题给 3min / 30s 逐字稿。
> **素材来源**（建景规 repo）：`docs/portfolio/项目档案_建景规规范问答助手.md` · `docs/PRD.md`（需求/竞品/用户/评测体系）· `docs/devlog/AIPM知识库总结.md`（主题化摘要，最新洞察见 `docs/devlog/2026-W7_*`，累计 **94 条**）· 各日开发日志。
> **诚实声明**：本项目是我**独立主导 + AI 辅助开发**的 MVP / 作品集项目，**未规模化上线**。凡涉及"使用人数、满意度、效率提升"等业务侧数据均为**设计目标 / 衡量框架**，无真实生产数据——面试时如实说明，不编造（见 §1.4、§1.7 的诚实处理）。
> **更新**：2026-W7（v2.2：补第五部分 工程与数据判断，启示 91-94）。

---

## 速查卡（开口前 30 秒扫一眼）

| 项 | 内容 |
|---|---|
| 一句话 | 我从 0 到 1 主导的"**像查法条一样查设计规范**"的 RAG 问答产品，所有回答附规范号/条文号/原文跳转，**严守不编造红线**。 |
| 定位 | 「AI 版规范法条数据库」，不是聊天伙伴。严谨权威、可溯源、不编造。 |
| 核心闭环 | 能问 → 能答 → 能溯源。 |
| 我的角色 | 产品负责人 + 决策者，AI 辅助实现。定义问题/定方案/定验收/卡红线/做迭代决策。 |
| 硬成果 | 综合质量分 **86.6 → 90.3**（首破 90）；单次 veto **38 → 13**；条文级检索 Hit@5 **78.8% → 89.8%**（零检索代码改动）；语料 **39 → 89 部 / 6 域 / 10785 chunks**；评测集 **50 → 171 条** + **48 条 Agent 路由集**；rerank **-55%**（零质量代价）；沉淀 **94 条**可迁移方法论、**143 项**自动化测试。 |
| 产品判断 | 强条分级高亮(红线可视化) · 项目工作区=预设规范范围 · 导出报告(可交付物) · 二级细分(导航成本 -45%) · 公式渲染诊断后**决定不做** 。详见 §4。 |
| 工程判断 | 先 profile 再优化(rerank 占 96%) · 省成本的第二条路=挪出关键路径 · 修复价值**事后用数据重新认定**。详见 §5。 |
| Agent 线 | 三个 Agent（查询分解 / 引用核验 / 工具调用）+ 一层规则调度（Router）+ 一套 **Agent 行为评测**（误触发率/漏触发率）。详见 §3。 |
| 4 条红线 | ①不编造 ②引用精确 ③强条用语（应/宜/可）不可错 ④不给 chunks 之外的设计建议。 |
| 为什么满意 | 它逼我把"AI 靠不靠谱"这种玄学问题，做成了**可检测、可拦截、可度量**的工程与评测。 |

---

# 第一部分 · 项目题（对应《面试内容梳理》§3）

## 1.1 介绍你最满意的一个项目（3 min 逐字稿）

**【背景 + 目标】**
设计规范体系非常庞大（2000+ 部），规划师日均要花 1–2 小时翻 PDF 查规范，效率低、还容易错。更要命的是：通用大模型查规范会**编造规范号、条文号、数字**——而这是个"像查法条一样"低容错的场景，编造就等于不可用。所以我立项做一款面向中型设计院规划师的 RAG 规范问答产品：用自然语言查规划/建筑/景观/消防/结构/市政 6 类规范，**每条回答都必须附可追溯的规范号、条文号和原文跳转**。

**【产品架构 · 一句话串流程】**
用户提问 → 场景识别 / 边界兜底 → BGE-M3 向量检索 Top20 → BGE-Reranker 重排 Top5 → 相关度阈值过滤 → DeepSeek 流式生成 → **后处理对齐**（校正量词/数字、剥离编造内容）→ SSE 推送答案 + 结构化引用卡 + 原文跳转。

**【我负责的工作（概述 3 句）】**
我是产品负责人：① 定义问题与边界、定 4 条产品红线；② 和"AI 研发"一起定 RAG 技术栈与分块/检索/重排策略阈值；③ 设计 7 维度评测 + 一票否决 + 方差分析，把红线做成可检测的工程后处理，并每轮看数据决定上线/回滚/砍功能。

**【成果（量化）】**
综合质量分从 86.6 迭代到 90.3（首破 90），单次一票否决从 38 砍到 13；条文级检索 Hit@5 从 78.8% 提到 89.8%——**而且是零检索代码改动，靠审计出评测集自身的标注错误做到的**；语料从 39 部扩到 89 部 / 6 域 / 10785 chunks；并沉淀了 94 条可迁移的 AI 产品方法论。

**【为什么满意（1 句）】**
因为它把"AI 到底靠不靠谱"这种听起来很玄的问题，落成了**可检测、可拦截、可度量**的产品与工程——这正是 AIPM 的核心价值。

## 1.2 项目中你承担了什么角色

产品负责人 + 决策者，AI 辅助实现。具体工作内容：
- **需求/业务**：用户调研、需求分析、业务梳理（6 类规范的场景矩阵）。
- **理想态定义**：定义每类场景"理想的回答长什么样"（结论先行、溯源完整、强条标识）。
- **评测体系构建**：7 维度评测 + 一票否决 + 方差分析。
- **架构设计**：与 AI 研发一起定技术栈与关键阈值（不是甩给研发，是共担决策）。
- **策略调优**：发现 badcase → 拆解 → 定位根因 → 给优化方案 → 验收。

> 诚实补充：本项目没有真实的算法/前后端团队，"研发/算法"角色由 AI 承担，我做"定义 + 审校 + 决策"。真正的**多角色团队协作**故事建议用**云上米轨**或工作经历来讲（见 §1.7、§2.5）。

## 1.3 项目的核心价值

- **用户价值**：把"查不到 / 看不懂 / 不敢用"三大痛点，变成"问一句就给答案 + 给原文 + 标强制性"，且**敢用于专业判断**（因为可溯源、不编造）。
- **产品差异化价值**：市场上"AI 智能 × 规范权威 × 可溯源跳转"三者的交集是空白——通用 LLM 会编造、规范电子版只能检索、垂类工具不深耕规范问答。我占的就是这个交集。
- **方法论价值**：沉淀了一套"红线工程化 + 评测先于优化"的可迁移打法。

## 1.4 算法指标 & 业务指标

**算法指标（先验 / 离线评估，有真实数据）**
- 检索层：Hit Rate@5、Recall@K、MRR、NDCG@K（目标 Hit Rate@5 ≥ 80%）。
- 生成层：7 维度 LLM Judge MOS 打分（0/1/2 三分制）+ 3 个红线维度的**一票否决（veto）召回**。
- 实际数据：综合分 86.6 → 88.3 → **90.3**；单次 veto 38 → 27 → **13**；dim4 原文用词准确率 86.2% → **94%**。

**业务指标（诚实口径）**
- 设计的衡量框架是：使用人数、用户满意度、规范查找效率（替代"翻 PDF 1–2 小时"）。
- **但本项目是 MVP / 作品集项目，未规模化上线，没有真实业务数据。** 面试如实说：业务指标是我**设计好的度量框架与目标值**，落地后才能采集。我能讲清楚"为什么这么定指标"，而不是编一个使用量。

## 1.5 有复盘吗？哪些可以提升

**产品侧**
- 业务侧验证缺位：MVP 只到离线评测，缺真实用户的在线行为数据闭环。
- 复杂场景（条文对比 ④、计算辅助 ⑤）属 V2/V3，MVP 只覆盖了 87% 高频流量。

**落地侧**
- 评测集 vs 真实场景仍有偏差（早期评测集偏理想化，后来才补对抗陷阱样本）。
- 调优时一度过度投入策略调参，**忽视了模型选型**这条更上游的杠杆。
- 上游数据是天花板：OCR 损伤的语料限制了上限，数据清洗不可能一次彻底。

> 复盘金句：**"绿色仪表盘可能只是没测到"**——指标全绿不等于没问题，可能是评测没覆盖到。

## 1.6 项目中遇到什么困难？如何解决（STAR）

**S/T**：通用 LLM 查规范会编造；且评测分本身有噪声、语料有 OCR 损伤——"改了感觉好了"根本不可信。

**A（三招）**
1. **红线工程化**：把"不编造/用词/数字"做成可检测的后处理——`post_filter` 剥离"补充说明"式编造、`align_modal_verbs` 校正"应/宜"、`align_numbers` 校回数字、dangling 引用监控。不靠 prompt 祈祷。
2. **噪声治理**：单次 LLM Judge 约一半是噪声，用**三次交集 + 方差分析**剥离判官噪声，才认定"真问题"。
3. **OCR 清洗**：把错字分"系统性 vs 随机弥散"，字典扩充靠"占用次数 + 上下文边界验证"——8 个候选过验证只留 2 个（淘汰 75%），避免误改合法字。

**R**：综合分稳定破 90，veto 砍到 13，红线类错误工程可控；并沉淀出"上游数据是天花板""数据干净≠产品更好"等方法论。

> **反直觉发现（高频追问弹药）**：我把 chunks 的 OCR 修干净后，**编造类错误反而上升**——干净文本让模型"更自信地发挥"。结论：数据质量↑ ≠ 产品质量↑，红线得靠工程兜底。

## 1.7 与算法、研发协作遇到的问题？如何解决

**诚实口径（必须先讲清）**：本项目是我独立主导 + AI 辅助，没有真实算法/研发同事。所以这题我分两层答：

**(a) 与 AI 的"协作"分工**：我定义问题、定方案、定验收、卡红线、做迭代决策；AI 负责实现与草稿。关键是我**能判断 AI 产出对不对、敢温柔挑战它的结论**——比如 AI 给的 align_numbers 方案（启示 62）我跑评测发现反而劣化，就果断关掉。AI 草稿 + 人审是不可分割的。

**(b) 可迁移到真实团队的协作原则**（也是我在云上米轨/工作中的做法）：一开始不懂技术方案不要硬装，自学补齐 → 针对某次 badcase 能清楚定位根因并给出方案、让研发/算法认可 → 之后才能持续平等地参与技术讨论。冲突时：控制情绪、聚焦问题、摆数据讲道理、必要时向上沟通找支持（详见 §2.5）。

## 1.8 怎么做竞品调研？对比竞品有哪些优势

**方法（4 步）**
1. **定目标**：这次竞品调研要回答"市场有没有人把'规范问答'做好"——聚焦效果与差异化空间。
2. **定竞品**：直接竞品（垂类规范工具）、间接竞品（通用 LLM、规范电子版、设计院内部 KB）。
3. **展开调研**：建对比矩阵，按"优势/不足"逐类拆。
4. **梳理结论**：找差异化空白。

**对比矩阵（实测结论）**

| 类型 | 代表 | 优势 | 不足 |
|---|---|---|---|
| 通用 LLM | ChatGPT/Claude/文心 | 通用强 | 不专、**易编造、无溯源** |
| 规范电子版 | 建工出版社/知圈 | 权威 | 只能文档检索、无智能问答 |
| 建筑垂类工具 | BIMBOX/品茗 | 行业熟 | 偏施工/造价，不深耕规范问答 |
| 院内 KB | 自建 wiki | 贴合项目 | 维护成本高、检索弱 |

**我的优势 / 差异化定位**：**AI 智能 × 规范权威 × 可溯源跳转** 三者交集——通用 LLM 缺权威与溯源，电子版缺智能，垂类缺深耕。这个交集是空白，就是我的位置。

---

# 第二部分 · 项目梳理（对应《面试内容梳理》§4）

## 2.1 用户调研

**如何确定用户**：从需求来源（行业数字化转型 + 规范体系庞大 + 我自己的规划专业背景）出发，锁定**中型设计院规划师**——专业、低容错、要可追溯依据；MVP 进一步聚焦"规划"方向（查询频次最高）。

**调研方式**

| 方法 | 对象 | 目的 |
|---|---|---|
| 深度访谈 | 1–10 年经验规划师 | 梳理痛点、了解场景 |
| 行为观察 | 真实查询过程 | 挖隐性需求 |
| 问卷调研 | 设计院从业者 | 验证痛点普遍性 |

**用户画像（虚构代表"王工"）**：设计院规划师，3 年经验，做控规/修详规/住区方案；每天花 1–2 小时查规范；用过 ChatGPT 但**不敢用于专业判断**；核心诉求＝**答案准确 + 能看原文 + 不编造**。

**痛点 3 大类**：① 查不到（不记得规范号、搜不到条文）② 看不懂（用词专业、不知强制还是推荐）③ 不敢用（LLM 看起来对但不敢信）。

**调研→产品的转化**：怕编造 → 评测设 2 项一票否决；要看原文 → 引用溯源 + 原文跳转必做；强条易遗漏 → 红线区分"应/宜/可"。

> 详版见建景规 repo `docs/PRD.md` 第〇部分·三《用户调研》。

## 2.2 竞品调研

见 §1.8（方法 + 矩阵 + 差异化）。一句话：**三者交集（AI×权威×溯源）是空白，就是我占的位**。详版见 `docs/PRD.md` 第〇部分·二《竞品调研》。

## 2.3 技术方案

**为什么选这套方案**

| 选型 | 选择 | 为什么 |
|---|---|---|
| 技术范式 | RAG | 规范是"有标准答案、要溯源"的知识库场景，RAG 比微调更可控、可溯源、可更新 |
| LLM | DeepSeek | 成本/中文/够用；该场景不需顶尖推理，需"听话不乱编、能被提示词约束"。**不为'看起来更强'换 GPT-4** |
| Embedding/Rerank | BGE-M3 / BGE-Reranker-v2 | 中文规范语义强、本地可控 |
| 向量库 | Qdrant（本地文件） | 轻量、带 payload 过滤（domain/spec_code）|
| 后端/前端 | FastAPI + SSE / React+TS+Vite+zustand | 流式、类型安全、轻状态 |

**守红线的工程手段**（方案的灵魂）：post_filter 剥离编造、align_modal_verbs 校正用词、align_numbers 校正数字、dangling 引用监控。

**当前方案的问题 / 可改进**：
- 优点：可溯源、可更新、红线可工程拦截、技术栈锁定不追新。
- 问题：上游数据（OCR）是天花板；复杂推理（条文对比/计算）MVP 未覆盖；检索召回仍是主要提升空间（曾试 BM25 混检索失败，见 §2.4）。

## 2.4 策略调优

**遇到的问题**：① 答案编造；② 评测分有噪声、不可信；③ 数据有 OCR 损伤。

**方法 + 效果**（见 §1.6 STAR）：后处理三件套（编造/用词/数字）→ dim4 原文用词 86.2%→94%、veto 38→13、综合分→90.3。

**失败实验的科学处置（加分项）**：BM25 混检索（启示 21）、few-shot 反例治编造（启示 58）、align_numbers（启示 62 修正 60）等多个失败实验，一律 **"保留代码 + 默认关 env flag + 文档解释原因"**——失败是资产不是垃圾，且能区分"诊断假设 ≠ 改造假设"。

**有没有更好的方案**：上游——补质量更高的语料 / 更强的 OCR 后校；检索——继续打磨召回（混检索方向需重新设计而非简单加权）；选型——把"模型选型"这条上游杠杆纳入下一轮（之前过度调参忽视了它）。

## 2.5 沟通协作

**本项目实情**：独立 + AI 辅助，无真实跨职能团队（见 §1.7）。**真实协作故事用云上米轨/工作经历讲**，方法论如下：

**遇到的问题类型**：跟算法/研发意见不合、技术方案一开始看不懂、决策推不动。

**怎么解决（4 步法，面试通用模板）**：
1. **控制情绪，聚焦问题**——对事不对人。
2. **摆事实（摆数据），讲道理**——用 badcase 数据和评测结果说话，而不是"我觉得"。
3. **向上沟通，寻找支持**——决策僵持时把信息透明给上级，让有权者拍板。
4. **自学补位建立信任**——不懂技术先自学，能精准定位一次根因、让研发认可，后续才有平等对话权。

---

# 第三部分 · 评测保真 与 Agent 线（W7 新增，v1.0 未覆盖）

> 这两条是我**最强的差异化**：队友的同类项目大概率没有。详版备答见
> [评测保真 QA](../评测保真-面试QA.md) 与 [Agentic RAG QA](../Agentic-RAG-面试QA.md)。

## 3.1 评测保真：我做过最有价值的"优化"，是没改模型

准备优化检索时，我先建可信基线，再逐条打开 29 例失败案例，读「评测认定的期望条原文」vs
「实际召回的原文」。结果：**约 1/3 是评测集自己标错了**——检索本来就对。
典型：问"什么是防火分区"，评测标的期望条其实是"防火间距"，而检索返回的才是"防火分区划分"。

处置：① 校准 10 条标注错误（不覆盖原集、逐条留证据）；② 引入**多值 GT**（幼儿园服务半径
GB50180 与 JGJ39 都合法，不能只认一个）。结果 **strict Hit@5 78.8% → 89.8%，零检索代码改动**。

**金句**：*"我没有优化一个坏掉的指标——我先证明了'提升空间'里大部分是评测自己标错的，剩下的才是真问题。"*

配套细节：我还自查出**审计脚本本身的 bug**（复合条文号被前缀匹配漏判成"数据缺失"），
复核后改判——**审计工具本身也要被审计**。

## 3.2 Agent 线：三个 Agent + 一层调度 + 一套 Agent 评测

| 层 | 打什么问题 | 关键实证 |
|---|---|---|
| **Router**（默认开） | 三个 Agent 各自对每条 query 收 LLM 税，导致全都"不敢开" | 规则路由 48 条 query 共 **0.2ms**、零成本 |
| 查询分解（默认开） | 发散/复合题单次检索只覆盖一个子话题 | 检索指标 0 变化，但喂给模型的片段从 1 个子话题→4 个 |
| 引用核验（暂缓） | 规则治理抓不到的语义编造 | **上线首条 query 即抓到红线违规**（见下）|
| 工具调用（默认开） | 精确查表类查询向量检索天生弱 | Router 对该类判定 **100% 召回 / 100% 精确** |

**最强的一个故事（3.2 必讲）**：模型把原文「**不应**把裸灯设置在视平线上」写成了「**应把**」——
漏一个"不"字，禁止性规定变成要求性规定，**语义完全反转**，正是我定义的红线 3 的硬违规。
规则式量词对齐**没抓到**，LLM verifier 抓到了。这把"规则打高频可模式化的错、LLM 兜语义级编造"
从理论变成了实证。

**Agent 评测（新 KPI）**：我原有检索/生成/兜底三层评测，唯独没评"Agent 决策对不对"。
新建 48 题路由评测集（含 10 题"措辞像发散实为单点"对抗样本），定义 **误触发率**（白付成本）
与 **漏触发率**（无损降级）。终版：准确率 93.8%、clear 样本 **100%**、漏触发 **0%**。

**一个克制的决策**：LLM 兜模糊区能把误触发 13.6%→4.5%，但 clear 样本从 100% 掉到 97.4%
且引入不确定性 → **默认保持纯规则，LLM 升级留 flag**。用数据决定，不用"看起来更强"决定。

## 3.3 追问：为什么有个 Agent 方向你做到一半停了？

原本要做"国标优先重排 boost"（解决地方标准挤掉国标）。诊断发现：那些国标条**根本没有被召回**
（在全库排第 47 / 93 名）——**boost 救不了一个不在候选集里的东西**。
于是我没写那行代码，把问题正确路由给了数据修复线（OCR 乱码）和评测复核。
**知道何时不做，也是判断力。**

---

# 第四部分 · 产品与交互判断（W7 新增）

> 前三部分讲技术怎么做，这部分讲**产品怎么判断**——AIPM 面试往往更看这条。
> 详版备答见 [产品与交互判断 QA](../产品与交互判断-面试QA.md)。

## 4.1 五个产品决策（都有取舍理由，不是"设计稿画了就做"）

| 决策 | 判断 |
|---|---|
| 强制性用语**分级**高亮 | 按法律效力分三档（强制红/推荐琥珀/允许灰），把红线 3 做成用户看得见的 |
| 项目工作区 = **预设规范范围** | 不做成会话文件夹；接到已有"多选规范限定"能力上，零后端改动、两功能互相放大 |
| 导出报告 | 把"可追溯"从体验变成**可交付物**（规范号+条文号+页码+强制性+现行状态+原文摘引）|
| 公式块渲染 → **决定不做** | 语料里公式已被 OCR 毁坏，渲染=把乱码包装得像模像样，违红线 1/2 |
| 规范**二级细分** | 把"看起来更清楚"变成可计算指标（期望扫描行数 10.4 → 5.7 行，-45%）；且**不是每个域都做**——4 部的域细分反而 +30% |
| 上传图纸查合规 → **砍掉** | 直接撞"不下合规结论"红线 |

## 4.2 最能讲的一件事：高亮上线，当场照出潜伏 bug

强条高亮一上线，我看到琥珀色的「宜」出现在奇怪位置——用户问"服务半径**不应**大于多少米"，
答案复述时变成了"**宜**大于多少米"，**产品改写了用户的提问**。
根因是早先为守红线 3 做的量词校正后处理**没有作用域概念**，把"复述用户问题"的那句也改了。
**为守红线而生的组件，在错误语境下自己制造了那类错误。**

修的时候踩到"第二次风险"：第一版阈值 0.6 看着修好了，但我在 **1802 条存档答案**上量影响面，
发现多数受影响的其实是"引用规范原文"——挡住它们等于**拦下 13 条合法的红线校正**。
改用数据定阈值（分布有干净断层：真引述 0.98 / 原文引用 ≤0.79），取 0.90。

**两条方法论**：① 可见化会反向暴露问题——观测做在 UI 上，bug 才会被看见；
② 定义自动化规则时必须同时定义**作用域**："改什么"和"在哪里不改"同样重要。

## 4.3 引入第三方库的决策纪律

只引过一次（Markdown 渲染）。做法：先说明代价（bundle gzip 70→116KB）、给出零依赖备选及其风险，
由负责人拍板后才装——项目规范写明"引入新库需先请示"。
改造中最需小心的不是排版，是**正文 `[N]` 引用角标（可追溯的唯一入口）不能被渲染器吃掉**；
验收标准是"好看了**且**原有关键交互一个不少"，我实际点了角标断言联动仍在——**可见≠可用**。

---

---

# 第五部分 · 工程与数据判断（W7 新增，启示 91-94）

> 这部分是"技术判断力"的证据：**怎么定位瓶颈、怎么省成本、怎么诚实认定一次修复的价值**。
> 详版备答见 [RAG-Context 工程 QA](../RAG-Context工程-面试QA.md) Q11-13 ·
> [Agentic-RAG QA](../Agentic-RAG-面试QA.md) Q13-14 · [评测保真 QA](../评测保真-面试QA.md) Q11-13。

## 5.1 先 profile，我原计划要做的事被证明是白做（启示 92）

Rerank 是链路最慢的一环。我原本打算做"并行子检索"，**先 profile 就发现它打在只占 4% 的环节上**：

| 阶段 | 耗时 | 占比 |
|---|---|---|
| embed | 0.59s | 4% |
| search | 0.06s | 0% |
| **rerank** | **14.46s** | **96%** |

然后用数据否掉两个看似合理的方案：**减候选 20→10**（37.5% 的选中结果变差 → 这是性能换质量，放弃）；
**调大 batch**（实测恰好相反：bs=2 1.98s < bs=8 2.84s < bs=40 3.76s）。

**"批越大越慢"违反 GPU 常识——我没绕过它，去追了根因**：`padding=True` 把整批补齐到该批最长序列，
规范 chunk 长短悬殊（几十字到 800 字），长短混批时短序列白算 padding。
理解机制后拿到的是**零代价优化**——**长度分桶**：80 pairs **11.41s → 5.08s（-55%）**，
**分数逐项差异 0.000000**。

**最值得讲的是因果链**：提速 → 发散题 13.7s → 11.1s → **进入 12s 核验预算** →
引用核验从"被跳过"变成"真的跑了" → **当场抓到红线 3 违规**（原文"**不应**把裸灯设置在视平线上"被写成"**应把**"）。
**性能优化直接换来了红线防护的覆盖率。**

## 5.2 省成本有两条路，我一开始只想到第一条（启示 91）

引用核验 Agent 因为成本一直默认关着。原计划是"按答案特征筛"——只核验数字/规范号密度高的答案。
我用 **1802 条存档答案**验证：**精确率从基线 22.1% 最多只提到 30.4%**——
因为规范类答案几乎都含数字（56%）、规范号（78%）、量词（89%），**这些特征没有区分度**。**假设被否掉。**

真正解决问题的是第二条路：**这件事不必在用户等待的路上做。**
核验的产出是徽章，不是答案——答案在核验开始前就流式输出完了。
而代码里原顺序是「核验 → 下发引用卡」，**用户白等 1.3s 才看到引用**。两处改动让它默认开：
① 引用卡提前到核验之前；② 预算感知触发（已耗时 ≥12s 则跳过，保 15s SLA）。

**方法论**：先分清**绝对成本**和**感知成本**（1.3s 没变，但感知等待减少了）；
**预算感知比特征筛选更诚实**——把不确定性交给约束，而不是交给猜测。

## 5.3 ⭐ 数据修复：修完发现价值不是我以为的那个（启示 94）

拿到一份 OCR 缺陷清单，**我照单开工的第一件事是复核，第一条就是误报**：
被标为「P0 直接毁答案」的两条术语，乱码的其实是 `type=appendix` 的**索引页** chunk，
正文完好；索引页只有「术语+页码」，短、中文占比低是**正确数据**。
端到端问一遍两题都答对——**省下两次"重新 OCR + 重新入库"**。

真缺陷是清单里没写的：**住建部页脚水印被 OCR 抓进正文，污染 1212/10785 条（11.2%）**，
其中 **928 条落在用户可见的引用卡原文摘引里**。已修（残留 0，向量全部重建）。

**但效果度量否掉了我动工时的两个理由，我按实测口径记录**：

| 我的假设 | 实测 |
|---|---|
| 清噪声会提升检索 | strict Hit@5 **89.78% → 89.78%，完全不变** |
| 断句会让 LLM 引错原文（红线 3）| A/B 同 temperature=0：**两版都正确引出原文，风险未发生** |

**已证实收益 = 原文摘引干净 + prompt 噪声 -1.21%，不是准确率提升。**
我保留这次修复但不改口径吹收益——产品定位是「可追溯原文」，引用里混水印本身就是缺陷。
**"该修"和"修了会涨指标"是两件事。**

> 顺带的指标素养：MRR 掉 1.5pp、4 条 query rank1→2 且 0 条变好，看着像回退。
> 查下去是 rerank 分数在 **0.99+ 饱和区**，分差只有 0.0002~0.0092，且 3/4 是**同规范相邻条文**互换。
> **饱和区的 top-1 排序本身不稳定，不足以据此接受或否决一次改动。**

## 5.4 一次红线保护的细节（能证明"我真的在守红线"）

清水印时，全语料含关键词的整行共 **24 种**，23 种是水印残片，
**但第 24 种是正经条文正文**："…2017 年国家发展改革委住房城乡建设部印发《关于规"。
**写成 `住房城乡建设部.*` 之类的正则就会删掉真条文。**
所以规则定成「整行 strip 后**完全等于**已核形态之一」才删，且**只整行删、不改任何保留行的字符**
——从机制上保证不可能动到规范用词。并为此专门写了一条单测：**提到住建部的真条文不能被删**。

# 附录 A · 高频追问弹药（数字分身/我可直接答）

- **Q：怎么保证 AI 不乱编规范？** 双层：检索层只让答案 grounded 在召回的真实条文；生成后用后处理拦截——剥离"补充说明"式编造、把被改写的"应/宜"和数字校回原文、监控越界引用；查不到就诚实说"未查询到"，绝不编。
- **Q：你怎么衡量好坏？** 7 维度评测（检索召回/精确条款/引用准确/原文用词/数字精确/边界识别/不编造），其中 3 个红线维度一票否决。关键：单次 LLM Judge 约一半是噪声，我用三次交集 + 方差分析才认定真问题。
- **Q：举一个反直觉发现？** 把 OCR 修干净后编造反而上升——干净文本让模型更自信发挥。数据质量↑ ≠ 产品质量↑。
- **Q：你做过最果断的产品决策？** 砍掉用户最想要的"上传方案查合规"——它直接撞"不下合规结论"红线 + OCR 质量风险，我把它降级为"列相关条文供自查"。**砍得清楚也是产品力**。
- **Q：你和 AI 怎么分工？** 我定义问题/定方案/定验收/卡红线/做迭代决策；AI 实现与草稿。我能判断对错、敢挑战它的结论。
- **Q：讲一个你的假设被数据否掉的例子？** 两个都能讲：①核验 Agent 想按"答案特征"筛，1802 条实测精确率 22.1%→最多 30.4%，特征没区分度；②修 OCR 水印时我预判"断句会让 LLM 引错原文"，A/B 证明 LLM 是鲁棒的、风险不存在。**假设被否掉时我重新认定价值，而不是改口径把数据说圆。**
- **Q：有没有做过"该做但不涨指标"的事？** 有。清掉 11.2% 语料的页脚水印污染，检索 Hit@5 一点没动。但 928 条 chunk 的引用原文摘引里混着"浏览专用"这种水印——**对一个主打"可追溯原文"的产品，这本身就是缺陷，不需要靠 KPI 提升给它找理由。**
- **Q：你怎么判断一个指标涨跌可不可信？** 先问"这个指标在这个区间还测不测得准"。例：rerank 分数挤在 0.99+ 饱和区时，top-1/MRR 的抖动是测量噪声不是质量信号——分差 0.001、换的是同规范相邻条文，才敢说无回退。

# 附录 B · 数字速记（背下来）

- 质量分：86.6 → 88.3 → **90.3**｜veto：38 → 27 → **13**｜dim4：86.2% → **94%**
- 条文级检索 Hit@5：78.8% → **89.8%**（零检索代码改动，全靠修评测）｜路由准确率 **93.8%**/clear **100%**/漏触发 **0%**
- 语料：39 → **89 部 / 6 域 / 10785 chunks**｜评测集：50 → **171 条** + **48 条 Agent 路由集**｜方法论：**94 条**｜自动化测试 **143 项**
- 性能：rerank **-55%**（80 pairs 11.41s→5.08s，分数差 0.000000）｜发散题 13.7s→**11.1s**（进入核验预算）｜简单题总时长 **3.8s**
- 数据修复：水印污染 **11.2%**（1212/10785）→ 残留 **0**；其中 **928 条**落在用户可见原文摘引｜导航成本 **-45%**（期望扫描 10.4→5.7 行）
- 性能目标：TTFT P95 ≤ 3s、总时长 ≤ 15s、吐字 ≥ 20 tokens/s、错误率 ≤ 1%
- 场景占比：查找 50% / 确认 20% / 解释 15%（L1 高频共 87%）

# 附录 C · 待回填 / 需本人确认

- [ ] **业务侧数据**：若后续真上线，补使用人数 / 满意度 / 效率提升真实值（当前无，勿编）。
- [ ] **用户调研样本量**：访谈"多名"的确切人数（PRD 用了模糊表述，面试若被追问，给真实数字或如实说"早期小样本访谈"）。
- [ ] **竞品调研深度**：如被追问具体竞品体验细节，参考 `docs/PRD.md` 及《竞品调研报告_v1.0》补充。
- [ ] 自检：以上数字与最新一轮评测/语料是否一致（以建景规 repo 最新 devlog 为准）。

---

**口径稿版本**：v2.2 · 2026-W7 · 对应项目 **94 洞察**（v2.1 基础上补第五部分：工程与数据判断，启示 91-94）
**配套**：建景规 repo `docs/portfolio/项目档案_建景规规范问答助手.md`（事实档案）· `docs/PRD.md`（需求/竞品/用户/评测全文）
